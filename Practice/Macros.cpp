#include <iostream>
#include <vector>
#include <utility>
#include <bits/stdc++.h>
using namespace std;

//Macros to shorten the code:
//A macro means that certain strings in the code will be changed before the compilation. In C++, macros are defined using the #define keyword.


//Single word shorten
#define F first;
#define S second;
#define PB push_back;
#define MP make_pair;

#define pi 3.174;

//For Function we don't need to use ';'
#define REP(a, i, b) for (int i = a; i <= b; i++)
#define SQ(a) (a) * (a)

int main() {
/* 
    v.PB(MP(y1, x1));
    v.PB(MP(y2, x2));
    int d = v[i].F + v[i].S;
 */

/* 
    REP(i,1,n) {
        search(i);
    }
 */
    cout<<SQ(3+3)<<"\n"<<pi;
    return 0;
}